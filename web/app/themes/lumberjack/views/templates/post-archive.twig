{% extends "base.twig" %}

{% block content %}
<main class="container mx-auto px-4" role="main">
    <div class="flex -mx-4 flex-wrap">
        <div class="mb-8 px-4 w-full md:mb-20 md:w-1/2 lg:w-4/12 flex items-center">
            <span class="text-5xl lg:text-6xl leading-tight font-gotu">See all these beautiful projects.</span>
        </div>
        {% for post in posts %}
        <div class="mb-20 px-4 w-full md:w-1/2 lg:w-4/12">
            {% if post.custom_thumbnail %}
            <div class="relative pb-9/16 mb-2">
                <img class="absolute w-full h-full object-cover" src="{{post.custom_thumbnail.url}}">
            </div>
            {% endif %}
            <div>
                <span class="text-sm uppercase font-barlow-condensed text-red tracking-widest font-semibold">
                    {{post.post_date | date("F d, Y") }}
                </span>
            </div>
            <div class="mb-2">
                <span class="text-3xl font-gotu">{{post.title}}</span>
            </div>
            <div class="mb-6">
                <p class="text-lg">{{post.custom_excerpt|excerpt(24)}}</p>
            </div>
            <a class="uppercase text-red font-semibold tracking-widest font-barlow-condensed" href="{{post.link}}">
                <span class="mr-2 hover:mr-4 transition-fast">Keep Reading</span>
                <span>&rarr;</span>
            </a>
        </div>
        {% endfor %}
    </div>
</main>
{% endblock %}